<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Màu nền bên trong -->
    <solid android:color="@color/color_surface_variant" />

    <!-- Viền quanh ảnh -->
    <stroke
        android:width="3dp"
        android:color="@color/color_accent" />

    <!-- Kích thước gợi ý (có thể bỏ) -->
    <size
        android:width="100dp"
        android:height="100dp" />
</shape>
