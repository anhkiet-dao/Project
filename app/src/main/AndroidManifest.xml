<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.do_an">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/avatar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Do_an"
        tools:targetApi="31">

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:ignore="WrongManifestParent">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Màn hình Đăng ký -->
        <activity
            android:name=".user.RegisterActivity"
            android:exported="false" />

        <activity android:name=".user.ForgotPasswordActivity"
            android:exported="false" />

        <activity android:name=".user.UserInfoActivity"
            android:exported="false" />

        <activity android:name=".application.NoteActivity"
            android:exported="false" />

        <activity android:name=".user.ProfileActivity"
            android:exported="false" />

        <activity android:name=".application.InforApp"
            android:exported="false" />

        <activity android:name=".UI.Account"
            android:exported="false" />

        <activity android:name=".API.AIReadingAssistant" android:exported="false" />
        <activity android:name=".API.AISummarizer" android:exported="false" />
        <activity android:name=".API.AIVoiceReader" />
        <activity android:name=".API.AIBookRecommender" android:exported="false" />
        <activity android:name=".API.GeminiAPIHelper" android:exported="false" />

<!--        <activity android:name=".user.LoginActivity"-->
<!--            android:exported="false" />-->

        <activity android:name=".application.ReadActivity"
            android:exported="false"/>

        <activity android:name=".UI.MyList"
            android:exported="false"/>

        <activity
            android:name=".user.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:exported="false" />

<!--        <activity android:name=".application.PDFListActivity"-->
<!--            android:exported="false" />-->

    </application>

</manifest>
